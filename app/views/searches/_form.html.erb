<%= form_tag("/search", :id => 'search-box', :method => "get", remote: true) do %>

  <div class="field">
    <%= label_tag (:artist) %><br />
    <%= text_field_tag (:artist) %>
  </div>

  <div class="actions">
    <%= submit_tag "Search", class: "btn btn-primary" %>
  </div>
<% end %>
