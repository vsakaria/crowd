<%= form_tag(@song, class: "remove-from-playlist-form", method: :delete) do |f| %>
  <%= submit_tag "Remove from Playlist", id: @song.id, :class => "remove-song-submit-btn" %>
  <%= label_tag :remove_from_playlist, @song.title %>
  <%= hidden_field_tag :artist_name, @song.artist_name %>
  <%= hidden_field_tag(( :song_id.to_s + "_#{index}"), @song.id, :class => "song_id") %>
  <%#= link_to "(remove)", song, method: :delete, data: { confirm: "Are you sure?" }, remote: true %>

<% end %>